# Alfa Project

Полнофункциональное веб-приложение с Django (backend) и React (frontend).

## Требования

- Docker
- Docker Compose
- Make

## Быстрый старт

### 1. Настройка переменных окружения

Файл `.env.dev` уже содержит настройки для локальной разработки. Для прода создайте файл `.env` из `.env.dev`:

```bash
cp .env.dev .env
```

И обязательно измените параметры безопасности:

**Django настройки:**
- `SECRET_KEY` - сгенерируйте новый уникальный секретный ключ
- `DEBUG` - установите `False` для продакшена
- `ALLOWED_HOSTS` - укажите реальные домены через запятую

**PostgreSQL настройки:**
- `POSTGRES_PASSWORD` - установите сложный пароль для базы данных
- `DB_PASSWORD` - установите тот же пароль (должен совпадать с `POSTGRES_PASSWORD`)

**React настройки:**
- `REACT_APP_API_URL` - путь к API (для прода может быть полный URL: `https://api.example.com`)

### 2. Запуск приложения

```bash
# Пересобрать образы и запустить (первый раз)
make build

# Или просто запустить
make run

# Запуск в фоновом режиме
make up
```

### 3. Создание суперпользователя

После запуска создайте администратора:

```bash
make createsuperuser
```

## Доступные команды

### Основные команды

- `make run` - запустить приложение (фронт + бэк + БД) с логами
- `make up` - запустить в фоновом режиме
- `make down` - остановить приложение
- `make down-volumes` - остановить и удалить все данные (включая БД)
- `make build` - пересобрать образы и запустить
- `make logs` - просмотр логов
- `make restart` - перезапустить приложение

### Работа с базой данных

- `make migrate` - применить миграции
- `make makemigrations` - создать новые миграции
- `make db-shell` - войти в PostgreSQL консоль
- `make db-reset` - полный сброс базы данных

### Django команды

- `make shell` - Django shell
- `make createsuperuser` - создать суперпользователя
- `make collectstatic` - собрать статические файлы

## Доступные адреса

После запуска приложение будет доступно по адресам:

- **Главная (через Nginx)**: http://localhost
- **Frontend**: http://localhost (проксируется через Nginx)
- **Backend API**: http://localhost/api (проксируется через Nginx)
- **Django Admin**: http://localhost/admin (проксируется через Nginx)
- **PostgreSQL**: localhost:5432

> Nginx работает как reverse proxy и проксирует все запросы на соответствующие сервисы

## Параметры базы данных

Параметры по умолчанию (из `.env.dev`):

- **База данных**: `alfa`
- **Пользователь**: `postgres`
- **Пароль**: `postgres`
- **Хост**: `db` (внутри Docker) / `localhost` (снаружи)
- **Порт**: `5432`

## Структура проекта

```
.
├── alfa/                  # Django backend
│   ├── alfa/             # Настройки Django
│   ├── marketing/        # Django приложение
│   ├── Dockerfile        # Docker образ для backend
│   └── requirements.txt  # Python зависимости
├── front/                # React frontend
│   ├── src/             # Исходники React
│   ├── public/          # Статические файлы
│   ├── Dockerfile       # Docker образ для frontend
│   └── package.json     # Node.js зависимости
├── nginx/                # Nginx reverse proxy
│   ├── nginx.conf       # Конфигурация Nginx
│   └── Dockerfile       # Docker образ для Nginx
├── docker-compose.dev.yml # Docker Compose для разработки
├── .env.dev             # Переменные окружения для разработки (не в git)
├── .env                 # Переменные окружения для продакшена (не в git, создается из .env.dev)
└── Makefile             # Команды для разработки
```

### Маршрутизация запросов (Nginx)

Nginx проксирует запросы следующим образом:

- `http://localhost/` → React Frontend (порт 3000)
- `http://localhost/api/*` → Django Backend (порт 8000)
- `http://localhost/admin/*` → Django Admin (порт 8000)
- `http://localhost/django-static/*` → **Nginx отдаёт напрямую** (Django static)
- `http://localhost/django-media/*` → **Nginx отдаёт напрямую** (Django media)

**Важно:** В React приложении используйте `REACT_APP_API_URL` для API запросов:

```javascript
// Пример использования в React
const API_URL = process.env.REACT_APP_API_URL || '/api';

fetch(`${API_URL}/endpoint`)
  .then(response => response.json())
  .then(data => console.log(data));
```

## Локальная разработка

### Изменение кода

- **Backend**: Изменения в `./alfa/` применяются автоматически благодаря hot-reload Django
- **Frontend**: Изменения в `./front/src/` применяются автоматически благодаря hot-reload React

### Подключение к базе данных

Для подключения к PostgreSQL из внешних инструментов (например, DBeaver, pgAdmin):

```
Host: localhost
Port: 5432
Database: alfa
Username: postgres
Password: postgres
```

### Логи

```bash
# Все логи
make logs

# Логи конкретного сервиса
docker-compose -f docker-compose.dev.yml logs -f backend
docker-compose -f docker-compose.dev.yml logs -f frontend
docker-compose -f docker-compose.dev.yml logs -f db
```

## Заметки

- `.env.dev` содержит настройки для локальной разработки и НЕ должен коммититься в git
- `.env` используется для продакшена, создается копированием из `.env.dev` с измененными параметрами безопасности
- Данные PostgreSQL хранятся в Docker volume `postgres_data` для персистентности
- При первом запуске миграции применяются автоматически

## 🔐 Генерация SECRET_KEY для продакшена

Для генерации нового SECRET_KEY выполните:

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```
